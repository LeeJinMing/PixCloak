{
  "name": "pixcloak",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm run build",
    "dev": "cd web && npm run dev",
    "start": "cd web && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
